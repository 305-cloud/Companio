# the core companion loop has zero hard dependencies beyond the stdlib
# (including belief.py's Bayesian confidence math -- no scipy required).
#
# install these to use --live and .env support:

google-adk>=2.8.0        # for llm.adk_backend.ADKBackend -- preferred --live backend
google-genai>=0.3.0        # for llm.gemini.GeminiBackend -- fallback if google-adk isn't installed
python-dotenv>=1.0.0          # so main.py can load .env automatically
pytest>=7.0                     # to run tests/
